#define ZERO 0
#define TEN 10
#define INVALID -1
#define PI 3.142
#define SQUARE(n) (n * n)
#define CUBE(n) (n * n * n)
/*
#define VOLUME_CUBE(n) ( n * n * n)
#define VOLUME_CUBOID(a, b, h) ( a * b * h)
#define VOLUME_CONE(h, r)    ( (PI * h * r * r) / 3)
#define VOLUME_SPHERE(r) ((PI * CUBE(r) ) * 4 / 3)
#define VOLUME_CYLINDER(r, h) ( PI * r * r * h)  */

int sum_of_digits(int );

int reverse_the_number(int );

int occurence_of_a_number(int ,int );

int is_palindrome(int );

int is_prime(int );

int first_n_primenumbers(int );

int sum_of_series( int , int );

int is_armstrong(int );

//int cube_of_a_number(int );

int is_amicable(int , int );

int divisor_sum(int );

int charge_of_electricity_used(int );

int menu_driven_for_arithmetic_operations(int a, int b);

float volume_of_given_shape();




